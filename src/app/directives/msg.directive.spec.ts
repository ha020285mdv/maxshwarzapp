import { MsgDirective } from './msg.directive';

describe('MsgDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgDirective();
    expect(directive).toBeTruthy();
  });
});
